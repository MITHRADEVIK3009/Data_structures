| Problem ID & Title | Key Operation/Approach | C++ Code Snippet |
|-------------------|------------------------|------------------|
| 136. Single Number | XOR all elements to find the unique one. | `int res = 0; for(int num : nums) res ^= num;` |
| 268. Missing Number | XOR indices and elements to find missing. | `int res = nums.size(); for(int i = 0; i < nums.size(); ++i) res ^= i ^ nums[i];` |
| 1720. Decode XORed Array | Cumulative XOR to decode the original array. | `vector<int> original(encoded.size() + 1); original[0] = first; for(int i = 1; i < original.size(); ++i) original[i] = original[i-1] ^ encoded[i-1];` |
| 1863. Sum of All Subset XOR Totals | Compute sum of XOR for all subsets (bitwise per bit or enumeration). | `int total = 0; for(int bit = 0; bit < 32; ++bit) { int count = 0; for(int num : nums) if(num & (1 << bit)) ++count; total += (1LL << bit) * count * (1 << (nums.size() - 1)); }` |
| 2917. Find the K-or of an Array | Bitwise OR of elements appearing >=k times (bit-by-bit count). | `int res = 0; for(int bit = 0; bit < 32; ++bit) { int count = 0; for(int num : nums) if(num & (1 << bit)) ++count; if(count >= k) res |= (1 << bit); }` |
| 2932. Maximum Strong Pair XOR I | Find max XOR of pairs with strength condition. | `int max_xor = 0; for(int i = 0; i < nums.size(); ++i) for(int j = i+1; j < nums.size(); ++j) if(abs(nums[i] - nums[j]) <= min(nums[i], nums[j])) max_xor = max(max_xor, nums[i] ^ nums[j]);` |
| 2980. Check if Bitwise OR Has Trailing Zeros | Check if OR of array is even (has trailing zero in binary). | `int or_val = 0; for(int num : nums) or_val |= num; return (or_val & 1) == 0;` |
| 3095. Shortest Subarray With OR at Least K I | Find shortest subarray with OR >=k (sliding window or brute). | `int min_len = INT_MAX; for(int i = 0; i < nums.size(); ++i) { int curr_or = 0; for(int j = i; j < nums.size(); ++j) { curr_or |= nums[j]; if(curr_or >= k) { min_len = min(min_len, j - i + 1); break; } } }` |
| 3158. Find the XOR of Numbers Which Appear Twice | XOR all numbers appearing exactly twice. | `unordered_map<int, int> freq; for(int num : nums) ++freq[num]; int res = 0; for(auto& p : freq) if(p.second == 2) res ^= p.first;` |
| 3314. Construct the Minimum Bitwise Array I | Construct min array from prefix OR values (bit manipulation). | `vector<int> res(prefix.size()); res[0] = prefix[0]; for(int i = 1; i < prefix.size(); ++i) res[i] = prefix[i] & ~prefix[i-1];` |
| 3688. Bitwise OR of Even Numbers in an Array | OR all even elements. | `int res = 0; for(int num : nums) if(num % 2 == 0) res |= num;` |
| 832. Flipping an Image | Reverse rows (position flip) and invert bits. | `for(auto& row : image) { reverse(row.begin(), row.end()); for(int& val : row) val ^= 1; }` |
| 1018. Binary Prefix Divisible By 5 | Compute prefix values as binary numbers mod 5. | `vector<bool> res; int prefix = 0; for(int num : nums) { prefix = (prefix * 2 + num) % 5; res.push_back(prefix == 0); }` |
| 2859. Sum of Values at Indices With K Set Bits | Sum elements where index popcount == k. | `int total = 0; for(int i = 0; i < nums.size(); ++i) if(__builtin_popcount(i) == k) total += nums[i];` |
| 2869. Minimum Operations to Collect Elements | Suffix operations to collect uniques from end. | `unordered_set<int> seen; int ops = 0; for(int i = nums.size() - 1; i >= 0; --i) { if(seen.size() < k && nums[i] <= k) seen.insert(nums[i]); if(seen.size() == k) { ops = nums.size() - i; break; } }` |
| 645. Set Mismatch | Count frequencies to find duplicate and missing. | `vector<int> freq(nums.size() + 1, 0); for(int num : nums) ++freq[num]; int dup, miss; for(int i = 1; i <= nums.size(); ++i) { if(freq[i] == 2) dup = i; if(freq[i] == 0) miss = i; }` |
| 1356. Sort Integers by The Number of 1 Bits | Sort elements by their bit count (popcount). | `sort(nums.begin(), nums.end(), [](int a, int b) { int ca = __builtin_popcount(a), cb = __builtin_popcount(b); return ca == cb ? a < b : ca < cb; });` |
| 1684. Count the Number of Consistent Strings | Check if all chars in string elements are allowed. | `unordered_set<char> allowed_set(allowed.begin(), allowed.end()); int count = 0; for(const string& word : words) { bool consistent = true; for(char c : word) if(allowed_set.find(c) == allowed_set.end()) { consistent = false; break; } if(consistent) ++count; }` |
| 2032. Two Out of Three | Find elements appearing in >=2 arrays. | `unordered_set<int> set1(nums1.begin(), nums1.end()), set2(nums2.begin(), nums2.end()), set3(nums3.begin(), nums3.end()); vector<int> res; for(int i = 1; i <= 100; ++i) if((set1.count(i) + set2.count(i) + set3.count(i)) >= 2) res.push_back(i);` |
| 2206. Divide Array Into Equal Pairs | Check if elements can be paired identically. | `unordered_map<int, int> freq; for(int num : nums) ++freq[num]; bool can_pair = true; for(auto& p : freq) if(p.second % 2 != 0) { can_pair = false; break; }` |
| 2506. Count Pairs Of Similar Strings | Count pairs where strings have same char sets. | `int count = 0; vector<bitset<26>> masks(words.size()); for(int i = 0; i < words.size(); ++i) for(char c : words[i]) masks[i].set(c - 'a'); for(int i = 0; i < words.size(); ++i) for(int j = i+1; j < words.size(); ++j) if(masks[i] == masks[j]) ++count;` |
| 3674. Minimum Operations to Equalize Array | Count min operations (e.g., to max or median). | `long long max_val = *max_element(nums.begin(), nums.end()); long long ops = 0; for(int num : nums) ops += max_val - num;` |
